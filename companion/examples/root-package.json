{
  "name": "my-monorepo",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml --env-file .env.dev --env-file .env.dev.secrets up --build --wait",
    "prod": "docker compose -f docker-compose.prod.yml --env-file .env.prod --env-file .env.prod.secrets up --build -d",
    "test": "pnpm run build && docker compose -f docker-compose.test.yml --env-file .env.test --env-file .env.test.secrets up -d --wait --build && pnpm run db:clean:test && pnpm run db:migrate:test && pnpm run test:unit && pnpm run test:e2e && docker compose -f docker-compose.test.yml down --volumes",
    "build": "turbo build",
    "lint": "pnpm --filter web lint",
    "db:generate": "prisma generate --schema ./packages/database/prisma/schema.prisma",
    "db:migrate:dev": "dotenv -e .env.dev -- prisma migrate dev --schema ./packages/database/prisma/schema.prisma",
    "db:clean:test": "docker exec project-db-test psql -U app -d app_test -c 'DROP SCHEMA public CASCADE; CREATE SCHEMA public;'",
    "db:migrate:test": "DATABASE_URL=postgresql://app:app@localhost:5433/app_test?schema=public prisma migrate deploy --schema ./packages/database/prisma/schema.prisma"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.11.0",
    "turbo": "^1.12.4",
    "typescript": "^5.2.2"
  }
}
